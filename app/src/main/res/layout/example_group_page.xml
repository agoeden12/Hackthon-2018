<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:background="@android:color/black"
        android:id="@+id/fake"
        android:text="            Group Chat"
        android:textColor="@android:color/white"/>

    <ImageView
        android:layout_below="@id/fake"
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="@drawable/black_border"
        android:padding="2dp"
        app:srcCompat="@drawable/feedhomeless"
        android:elevation="2dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/userTypingEditText"
        android:orientation="horizontal"
        android:layout_margin="16dp"
        android:elevation="12dp">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/login_username_image" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/offWhite"
            android:textColor="@color/darkRed"
            android:text="This Layout Is Style In Designing Stage..."/>

    </LinearLayout>

    <EditText
        android:id="@+id/userTypingEditText"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:layout_margin="10dp"
        android:hint="Write Your Message..."
        />
</RelativeLayout>